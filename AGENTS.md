## Learned User Preferences
- Keep admin and user UI clearly separated, without mixed navigation or features.
- Prioritize consistent visual design across pages, especially sidebar, layout, and action controls.
- Implement requested changes directly in code rather than only proposing plans.
- When project docs are provided, use them as the source of truth for implementation.

## Learned Workspace Facts
- `src/API.md` is the backend contract reference expected for endpoint integration.
- The design guideline source is under `stitch_backend_overview_design_requirements` and is used for frontend alignment.
